
export interface currentConditions {
  hoursToDisplay: number,
  sunrise: string,
  sunset: string,
  condition: string
  address: string,
  temp: number,
  maxtemp: Number,
  mintemp: number,
  wind: number,
  humidity: number,
  pressure: number
  visibility: number,
  cloud: number,
  feelslike: number,
  icon: string,
  description: string,
  ampm:string
}